# Exemplo de configuração HTTPS para o arquivo .env

# ======================================
# 🔒 CONFIGURAÇÕES HTTPS
# ======================================

# Habilitar HTTPS globalmente
USE_HTTPS=true
SSL_ENABLED=true

# Domínio principal (em produção)
DOMAIN=bigdata.meudominio.com
SERVER_HOST=bigdata.meudominio.com

# Proxy reverso (Nginx, Traefik, etc.)
USE_REVERSE_PROXY=true

# ======================================
# 🔒 HTTPS POR SERVIÇO (opcional)
# ======================================

# Airflow HTTPS
AIRFLOW_HTTPS=true
AIRFLOW_SSL_CERT=/etc/ssl/certs/airflow.crt
AIRFLOW_SSL_PRIVATE=/etc/ssl/private/airflow.pem

# MinIO HTTPS
MINIO_HTTPS=true
MINIO_SSL_CERT=/etc/ssl/certs/minio.crt
MINIO_SSL_PRIVATE=/etc/ssl/private/minio.pem

# Jenkins HTTPS
JENKINS_HTTPS=true
JENKINS_SSL_CERT=/etc/ssl/certs/jenkins.crt
JENKINS_SSL_PRIVATE=/etc/ssl/private/jenkins.pem

# Jupyter HTTPS
JUPYTER_HTTPS=true
JUPYTER_SSL_CERT=/etc/ssl/certs/jupyter.crt
JUPYTER_SSL_PRIVATE=/etc/ssl/private/jupyter.pem

# ======================================
# 📋 EXEMPLOS DE USO
# ======================================

# DESENVOLVIMENTO LOCAL (HTTP):
# USE_HTTPS=false
# SERVER_HOST=localhost
# DOMAIN=

# PRODUÇÃO COM PROXY REVERSO (HTTPS):
# USE_HTTPS=true
# DOMAIN=bigdata.empresa.com
# USE_REVERSE_PROXY=true

# PRODUÇÃO COM CERTIFICADOS DIRETOS:
# USE_HTTPS=true
# DOMAIN=bigdata.empresa.com
# USE_REVERSE_PROXY=false
# AIRFLOW_SSL_CERT=/etc/ssl/certs/airflow.crt
# MINIO_SSL_CERT=/etc/ssl/certs/minio.crt
# (etc...)

# ======================================
# 🌐 COMPORTAMENTO DO make status
# ======================================

# Com essas configurações, o comando `make status` irá:
# 1. Detectar automaticamente HTTPS vs HTTP
# 2. Usar o domínio correto (DOMAIN vs localhost)
# 3. Ajustar portas para proxy reverso
# 4. Mostrar URLs corretas para cada ambiente
# 5. Exibir informações sobre configuração SSL

# Exemplos de URLs geradas:
# Desenvolvimento: http://localhost:8080
# Produção simples: https://bigdata.empresa.com:8080
# Produção com proxy: https://bigdata.empresa.com/airflow