# Exemplo de configuração - copie para .env e ajuste conforme necessário

# Configurações de Plataforma
# Valores possíveis: linux/amd64, linux/arm64
# GitHub Codespaces: linux/amd64
# Mac M1/M2: linux/arm64
# Intel/AMD: linux/amd64
DOCKER_PLATFORM=linux/amd64

# Configurações do Servidor
SERVER_IP=192.168.1.22
SERVER_HOST=localhost

# Configurações do Airflow
AIRFLOW_UID=1000
AIRFLOW_GID=0

# Configurações do PostgreSQL
POSTGRES_USER=airflow
POSTGRES_PASSWORD=sua_senha_postgres_aqui
POSTGRES_DB=airflow

# Configurações do Airflow
AIRFLOW_FERNET_KEY=sua_fernet_key_aqui
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=sua_senha_airflow_aqui

# Configurações do MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=sua_senha_minio_aqui

# Configurações do Jenkins
JENKINS_ADMIN_USER=admin
JENKINS_ADMIN_PASSWORD=sua_senha_jenkins_aqui

# Configurações de Rede
# Para detectar portas automaticamente: make detect-ports
# Portas padrão (altere se houver conflitos)
AIRFLOW_PORT=8080
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
JUPYTER_PORT=8888
SPARK_UI_PORT=8081
SPARK_LOCAL_UI_PORT=4040
JENKINS_PORT=8082
FLOWER_PORT=5555

# Configurações do Spark
SPARK_MASTER_HOST=spark-master
SPARK_MASTER_PORT=7077
SPARK_MASTER_URL=spark://${SPARK_MASTER_HOST}:${SPARK_MASTER_PORT}
SPARK_APPS_PATH=/opt/bitnami/spark/apps

# URLs Base (não edite - são calculadas automaticamente)
AIRFLOW_BASE_URL=http://${SERVER_IP}:${AIRFLOW_PORT}
MINIO_SERVER_URL=http://${SERVER_IP}:${MINIO_API_PORT}
MINIO_BROWSER_REDIRECT_URL=http://${SERVER_IP}:${MINIO_CONSOLE_PORT}

# Configuração de Plataforma Docker
# Valores: linux/amd64 (padrão), linux/arm64 (Mac M1/M2)
# Use 'make detect-platform' para detectar automaticamente
DOCKER_PLATFORM=linux/amd64